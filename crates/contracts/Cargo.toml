[package]
name = "coxagent-contracts"
version = "0.0.0"
edition = "2021"
license = "MIT"
description = "Shared wire contracts (DTOs + events) between CoXAgent services"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
