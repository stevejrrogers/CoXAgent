# One-shot CoXAgent deploy: app + MinIO (file storage) + coturn (calls) + Caddy
# (automatic HTTPS). Run on a Linux host with a PUBLIC IP and a DOMAIN pointing
# at it. Copy .env.example to .env, fill it in, then:  docker compose up -d
#
# Firewall: open 80/tcp, 443/tcp (Caddy), 3478/tcp+udp and 49160-49200/udp
# (coturn). MinIO is internal-only (the app proxies file bytes).
name: coxagent

services:
  minio:
    image: minio/minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${S3_ACCESS_KEY:?MinIO access-key must be set}
      MINIO_ROOT_PASSWORD: ${S3_SECRET_KEY:?MinIO credential must be set}
    volumes:
      - minio:/data
    restart: unless-stopped

  mongo:
    image: mongo:7
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USER:?MongoDB root user must be set}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD:?MongoDB root secret must be set}
    volumes:
      - mongo:/data/db
    restart: unless-stopped

  # Shared state for the distributed runtime: many hubs (machines) read/write one
  # source of truth. Ticket claims are transactional here.
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${PG_USER:?Postgres role must be set}
      POSTGRES_PASSWORD: ${PG_PASSWORD:?Postgres superuser secret must be set}
      POSTGRES_DB: coxagent
    volumes:
      - db:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${PG_USER:?Postgres role must be set}"]
      interval: 5s
      timeout: 3s
      retries: 10
    restart: unless-stopped

  # Ephemeral coordination: leader election, per-ticket stage leases, and the
  # worker registry — fast TTL keys. Durable data stays in Postgres.
  redis:
    image: redis:7-alpine
    command: ["redis-server", "--save", "", "--appendonly", "no"]
    restart: unless-stopped

  coturn:
    image: coturn/coturn
    # Host networking: coturn needs the public IP and a wide UDP relay range.
    network_mode: host
    command:
      - -n
      - --use-auth-secret
      - --static-auth-secret=${TURN_SECRET:?coturn shared-secret must be set}
      - --realm=coxagent
      - --no-tls
      - --no-dtls
      - --min-port=49160
      - --max-port=49200
      - --external-ip=${PUBLIC_IP}
    restart: unless-stopped

  app:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    environment:
      COXAGENT_HOST: 0.0.0.0
      COXAGENT_ADMIN_USER: ${ADMIN_USER:?superuser login must be set}
      COXAGENT_ADMIN_PASSWORD: ${COXAGENT_ADMIN_PASSWORD:?a strong super-admin secret must be set}
      # File storage → MinIO (over the internal docker network).
      COXAGENT_S3_ENDPOINT: http://minio:9000
      COXAGENT_S3_BUCKET: coxagent
      COXAGENT_S3_ACCESS_KEY: ${S3_ACCESS_KEY:?MinIO access-key must be set}
      COXAGENT_S3_SECRET_KEY: ${S3_SECRET_KEY:?MinIO credential must be set}
      # Documentation store → MongoDB (server-side system of record for docs).
      COXAGENT_MONGO_URL: mongodb://${MONGO_USER:?MongoDB root user must be set}:${MONGO_PASSWORD:?MongoDB root secret must be set}@mongo:27017/?authSource=admin
      COXAGENT_MONGO_DB: coxagent
      # Shared state → Postgres; ephemeral coordination → Redis. Set both on every
      # hub/machine so N teams coordinate (claim tickets, elect a leader) safely.
      COXAGENT_DB_DSN: postgres://${PG_USER:?Postgres role must be set}:${PG_PASSWORD:?Postgres superuser secret must be set}@db:5432/coxagent
      COXAGENT_REDIS_URL: redis://redis:6379
      # TURN for calls — the browser reaches coturn at the PUBLIC IP directly.
      COXAGENT_TURN_URL: turn:${PUBLIC_IP}:3478
      COXAGENT_TURN_SECRET: ${TURN_SECRET:?coturn shared-secret must be set}
      COXAGENT_REGISTRY: /data/registry.json
    volumes:
      - appdata:/data
      - ./data/TL:/data/TL:rw
      - ./data/registry.json:/data/registry.json:rw
    depends_on:
      - minio
      - mongo
      - db
      - redis
    restart: unless-stopped

  caddy:
    image: caddy:2
    ports:
      - "80:80"
      - "443:443"
    environment:
      DOMAIN: ${DOMAIN}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - app
    restart: unless-stopped

volumes:
  minio:
  mongo:
  db:
  appdata:
  caddy_data:
  caddy_config:
