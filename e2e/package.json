{
  "name": "coxagent-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "sh -c '. ./fixture-guard.sh && clear_fixture_ports && exec npx playwright test'",
    "test:auth": "sh -c '. ./fixture-guard.sh && clear_fixture_ports && exec npx playwright test -c playwright.auth.config.ts'",
    "baseline": "sh -c '. ./fixture-guard.sh && clear_fixture_ports && exec npx playwright test --update-snapshots'"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
